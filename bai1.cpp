#include <bits/stdc++.h>
using namespace std;
int main() {
    int t; cin>>t;
    while (t--) {
        int x; cin>>x;
        int y; cin>>y;
        vector<int> a(x);
        vector<int> b(y);
        for (int i = 0; i < x; i++) {
            cin>>a[i];
        }
        for (int i = 0; i < y; i++) {
            cin>>b[i];
        }

    }
}